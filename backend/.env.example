### Option A: Full connection string (recommended)
# Replace <password> and optionally set a DB name via MONGODB_DB
# MONGODB_URI=mongodb+srv://talhaazfar:<password>@cluster0.sl1gjec.mongodb.net/myDatabase?retryWrites=true&w=majority

### Option B: Provide only the password (the code will construct the URI)
# MONGODB_PASSWORD=<password>
# Optionally set a default DB name
# MONGODB_DB=myDatabase
